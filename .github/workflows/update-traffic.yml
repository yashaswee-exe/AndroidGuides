- name: Generate Dashboard
  run: |
    REPO="yashaswee-exe/AndroidGuides"

    VIEWS=$(curl -s -H "Authorization: token ${{ secrets.TRAFFIC_TOKEN }}" \
      https://api.github.com/repos/$REPO/traffic/views | jq '.count')

    UNIQUES=$(curl -s -H "Authorization: token ${{ secrets.TRAFFIC_TOKEN }}" \
      https://api.github.com/repos/$REPO/traffic/views | jq '.uniques')

    CLONES=$(curl -s -H "Authorization: token ${{ secrets.TRAFFIC_TOKEN }}" \
      https://api.github.com/repos/$REPO/traffic/clones | jq '.count')

    UNIQUE_CLONES=$(curl -s -H "Authorization: token ${{ secrets.TRAFFIC_TOKEN }}" \
      https://api.github.com/repos/$REPO/traffic/clones | jq '.uniques')

    cat <<EOF > Dashboard.md
# 📊 AndroidGuides Analytics Dashboard

> Repository: yashaswee-exe/AndroidGuides  
> Auto-updated every 6 hours  

---

## 👀 Traffic Overview (Last 14 Days)

| Metric | Value |
|--------|-------|
| 🔎 Total Views | **$VIEWS** |
| 👤 Unique Visitors | **$UNIQUES** |
| 📥 Total Clones | **$CLONES** |
| 🧑‍💻 Unique Cloners | **$UNIQUE_CLONES** |

---

## 📈 Quick Stats

- Views per day (approx): **$((VIEWS / 14))**
- Clones per day (approx): **$((CLONES / 14))**

---

_Last Updated: $(date -u) UTC_

EOF
